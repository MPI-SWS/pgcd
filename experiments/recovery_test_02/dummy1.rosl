x=0;
wait(1);
if (x==0) {
    send(dummy2, Red);
    fail(1,2);
    send(dummy2, Done);
} else {
    send(dummy2, Green);
    receive(dummy2, idle) {
        case Done() =>
            exit(0);
    }
}
